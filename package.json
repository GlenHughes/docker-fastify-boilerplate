{
  "name": "docker-fastify-boilerplate",
  "version": "1.0.0",
  "description": "docker fastify boilerplate app",
  "author": {
    "name": "Glen Hughes",
    "email": "glen.hughes@iproov.com"
  },
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon server/server.js",
    "bundle": "esbuild src/app.js --bundle --outfile=dist/app.bundle.js --minify --sourcemap"
  },
  "dependencies": {
    "fastify": "^4.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.11",
    "nodemon": "^2.0.22"
  }
}
